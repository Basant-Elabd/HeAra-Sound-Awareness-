# ============================================================
# HeAra — AI Backend Requirements
# Covers: FastAPI server, Main Model (VGGish+OpenL3+DNN),
# Emergency Mode (Conv1D/TFLite), Custom Sounds (YAMNet+wav2vec2)
# ============================================================

# --- Web framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4

# --- Firebase / Firestore ---
firebase-admin==6.5.0

# --- Deep learning ---
tensorflow==2.15.0
tensorflow-hub==0.16.1
torch==2.3.1
transformers==4.42.3

# --- Audio embeddings & processing ---
openl3==0.4.2
resampy==0.4.3
librosa==0.10.2.post1
soundfile==0.12.1

# --- ML utilities ---
scikit-learn==1.5.0
numpy==1.26.4
pandas==2.2.2
joblib==1.4.2

# --- Misc ---
python-dotenv==1.0.1
requests==2.32.3

# NOTE: pin versions above are a reasonable baseline based on the stack in
# use (VGGish/OpenL3, YAMNet, wav2vec2, TFLite conversion). Run:
#   pip freeze > requirements.txt
# on your working machine before pushing, to capture the exact versions
# that are actually installed and tested.
